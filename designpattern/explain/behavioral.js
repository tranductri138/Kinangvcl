/**
 * Behavioral Design Patterns
 * Interpreter, Template Method, Chain of Responsibility, Command,
 * Iterator, Mediator, Memento, Observer, State, Strategy và Visitor.
 * Nhóm này dùng trong thực hiện các hành vi của đối tượng,
 * sự giao tiếp giữa các object với nhau.
 * */